# Frontend

Vite + React + TypeScript SPA for Slide Maker.

```bash
npm install
npm run dev      # http://localhost:5173 — proxies /api → :8000
npm run build
npm run lint
```

See the [root README](../README.md) for full project setup.
